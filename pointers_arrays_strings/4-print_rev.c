#include <stdio.h>
#include <string.h>
/**
 *print_rev- prints a string, in reverse, followed by a new line.
 *@s: char
 *Return: 0
 */
void print_rev(char *s)
{
len = strlen(s) 
for (i = len - 1; i >= 0; i--)
{
printf(s[i])
} 
printf('/n')
}
