#include "main.h"
/**
 *_isupper- checks for uppercase character.
 * @c:integer
 * Return:Always 0.
*/
int _isupper(int c);
if {
	if (c >= 65 && c <= 90)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
